[build]
buildCommand = "python -m pip uninstall -y bson || true; python -m pip install --no-cache-dir -r requirements.txt"

[deploy]
startCommand = "python -m app.bot"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
